import React from 'react';
import { Card, makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
  root: {},
}));

const ProjectCard = ({}) => {
  classes = useStyles();

  return <Card> Project card</Card>;
};

export default ProjectCard;
